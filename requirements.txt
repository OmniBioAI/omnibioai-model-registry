# Model Registry IAM Integration: centralized JWT verification via the
# shared IAM client, replacing the local HS256-only decoder auth.py used
# to have. This editable local-path install is for local bare-metal dev
# only -- run this file before `pip install -e .` so the
# omnibioai-iam-client requirement declared in pyproject.toml is already
# satisfied and pip doesn't try to fetch it from an index.
#
# The Docker build no longer uses this file for omnibioai-iam-client;
# the Dockerfile now vendor-installs it directly from the sibling repo
# (same pattern as omnibioai-lims/omnibioai-rag).
-e ../omnibioai-iam-client

# PR14.2B-3: same local-dev-only editable-install convention, for
# omnibioai-usage-client.
-e ../omnibioai-usage-client
